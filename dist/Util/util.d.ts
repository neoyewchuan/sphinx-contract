export default class SphinxUtils {
    static calculateWarrantyEndDate(warStart: string, warPeriod?: number): string;
    static dayInMonth(theYear: number, theMonth: number): number;
    static formatDateYMD(date: Date): string;
}
